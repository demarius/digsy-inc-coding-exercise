<div class="alert"><%= flash[:action]%></div>

<h2>New Action</h2>

<%= form_for @new_action, html: {class: 'ink-form all-50 small-100 tiny-100'} do |f| %>

  <div class="control-group required">
    <%= f.label :icon %>
    <div class="control">
        <%= f.select :icon, @new_action.icons %>
    </div>
  </div>

  <div class="control-group required">
    <%= f.label :name %>
    <div class="control">
      <%= f.text_area :name %>
    </div>
  </div>
  <div class="control-group column-group">
    <div class="control all-80">&nbsp;
    </div>
    <div class="control all-20">
      <%= f.submit "Create", class: 'ink-button' %>
    </div>
  </div>
<% end %>

<%= link_to "Cancel", agent_actions_path %>

